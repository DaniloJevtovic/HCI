<Window x:Class="Projekat.Vrsta.OdabirEtikete"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Icon="/Projekat;component/Images/logo.png"
        Title="Odaberite etiketu" Height="300" Width="300" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="10*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DataGrid Grid.Row="0" x:Name="EtiketeTabelaOdb" ItemsSource="{Binding Tipovi, UpdateSourceTrigger=PropertyChanged}"  IsReadOnly="True" AutoGenerateColumns="False" Height="Auto">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Oznaka" Binding="{Binding Oznaka, UpdateSourceTrigger=PropertyChanged}" Width="3*"/>
                <DataGridTextColumn Header="Boja" Binding="{Binding Boja, UpdateSourceTrigger=PropertyChanged}" Width="3*"  />
            </DataGrid.Columns>
        </DataGrid>

        <Button Grid.Row="1" Content="Odaberite" Click="Odaberi_Click"></Button>
    </Grid>
</Window>
